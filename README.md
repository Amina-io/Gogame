
  # Minimal Terminal Interface

  This is a code bundle for Minimal Terminal Interface. The original project is available at https://www.figma.com/design/mVxd6gE3yROWgybRr8tRfB/Minimal-Terminal-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  